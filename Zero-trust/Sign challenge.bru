meta {
  name: Sign challenge
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/auth/sign
  body: json
  auth: inherit
}

body:json {
  {"challenge":"{{CHALLENGE}}"}
}

script:post-response {
  bru.setEnvVar("SIGNATURE", res.body.signature)
}

settings {
  encodeUrl: true
  timeout: 0
}
